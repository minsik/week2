<!-- nav bar doesn't do anything but added anyway -->
<%= link_to "Friends", friends_url %>
<%= link_to "Favorites", favorites_url %>
<%= link_to "Greeting", greeting_url %>
<%= link_to "Search", search_url %>

<h1>Favorites</h1>
<ul>
  <% @favorites.each do |fav| %>
    <li><%= fav %></li>
  <% end %>
</ul>
